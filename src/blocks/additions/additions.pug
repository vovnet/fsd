mixin additions(data)
  - var noBorder = '';
  if data.noBorder
    - noBorder = 'additions--no-border'
  .additions(class=noBorder)
    .additions__title= data.title
    div= data.desc
    i.additions__icon.material-icons= data.icon