mixin checkbox({ label, checked=false, bold=false })
  - let classes = ["checkbox"]
  if bold
    - classes.push("checkbox--text-bold")
  label(class=classes)= label
    input.checkbox__input(type="checkbox" checked=checked)
    .checkbox__mark
